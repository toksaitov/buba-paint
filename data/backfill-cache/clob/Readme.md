# CLOB Backfill Cache

This directory is reserved for cached CLOB API responses used by historical backfill workflows.

It is currently empty. Keep the directory so cache layout stays explicit next to the populated Gamma cache.

This is derived cache space, not primary run evidence.
