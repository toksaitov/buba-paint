# Finalization Evidence

This directory contains the finalization manifest, pre/post Docker state,
SQLite checkpoint output, remote storage notes, and checksum files for the
`live-readonly-20260514-184119` freeze.

Use `runtime-SHA256SUMS` as the source of truth for the copied runtime file
digests. `local-SHA256SUMS` records the local verification pass.
