# Replay-Grade Readonly Soak 002

Status: abandoned detached-run attempt, not valid soak evidence.

This directory was created while trying to run the Phase 9 host soak in the background from the local machine. The detached local wrapper exited before a complete host soak could run, so this directory is retained only as provenance for the failed launch attempt.

Do not use this directory as accepted Phase 9 evidence. The next meaningful evidence directory is `data/experiments/replay-grade-readonly-soak-003/`, which contains the attached runner failure and the follow-up 5-minute auth-stability finding.
