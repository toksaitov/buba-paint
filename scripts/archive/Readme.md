# Archived Scripts

This directory contains one-off scripts kept for historical run analysis. They are not maintained as current workflows.

If an archived script becomes useful again, move the reusable version back to `scripts/` with current docs and tests.

- `run-006/`: old run-specific forensic scripts.
- `demo/`: old dashboard demo SQL for the removed six-table-only workflow.
