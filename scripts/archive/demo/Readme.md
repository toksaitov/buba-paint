# Demo Archive

`seed-demo-db.sql` is an old dashboard fixture for the original six-table schema. It is kept for provenance only and should not be used as the current dashboard or replay-grade schema template.

